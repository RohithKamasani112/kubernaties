Part 1: Angular Fundamentals
What is Angular

A component-based front-end framework by Google

Uses TypeScript

Complete solution: routing, state, HTTP, forms

Setting Up Angular

Install Angular CLI: npm install -g @angular/cli

Create project: ng new my-app

Angular CLI commands (ng serve, ng generate, ng build)

Project Structure Overview

src/app, main.ts, app.module.ts

Components, modules, services

Components

Creating components: ng generate component

Component decorator: @Component

Template (.html), Style (.css), Class (.ts)

Data binding (interpolation, property binding, event binding)

⚙️ Part 2: Templates, Data Binding, and Events
Types of Data Binding

Interpolation: {{ title }}

Property binding: [src]="imgUrl"

Event binding: (click)="handleClick()"

Two-way binding: [(ngModel)]="value"

Directives

Structural: *ngIf, *ngFor, *ngSwitch

Attribute: [ngClass], [ngStyle]

Custom directives (@Directive)

Event Handling

Event objects ($event)

Event bubbling and custom event emitters

🧱 Part 3: Component Communication
@Input() and @Output()

Passing data from parent → child

Sending events child → parent

ViewChild & ContentChild

Access child components & templates

Component Lifecycle Hooks

ngOnInit, ngOnChanges, ngAfterViewInit, etc.

📦 Part 4: Angular Modules and Services
NgModules

Feature modules vs root module

@NgModule decorator

Declarations, imports, providers, exports

Services & Dependency Injection

Create a service: ng generate service

Injecting with constructor

Singleton behavior of services

HttpClient (Built-in)

Make HTTP requests with HttpClientModule

GET, POST, PUT, DELETE

Observables with HttpClient

📝 Part 5: Angular Forms (Template & Reactive)
Template-Driven Forms

FormsModule

ngModel, #templateRef, form validation

Reactive Forms

FormControl, FormGroup, FormBuilder

Validation: required, minlength, pattern

Dynamic forms with arrays

Form Validation

Built-in validators

Custom validators

🧭 Part 6: Routing and Navigation
Angular Router

Set up routes: RouterModule.forRoot([])

<router-outlet>, routerLink, router.navigate()

Route Params & Query Params

ActivatedRoute

params, snapshot, queryParams

Route Guards

CanActivate, CanDeactivate, Resolve

Protecting routes and lazy loading modules

Lazy Loading Modules

Improve performance by loading modules only when needed

⚙️ Part 7: Advanced Angular Features
Observables & RxJS

Observables vs Promises

Operators: map, switchMap, filter, mergeMap, tap

Subscriptions and async pipe

State Management (Optional Advanced)

Services for local state

NGRX (Redux-style for Angular)

Pipes

Built-in pipes: date, currency, uppercase

Custom pipes (@Pipe)

Custom Structural/Attribute Directives

Build reusable UI logic

🚀 Part 8: Testing, Deployment & Performance
Testing in Angular

Unit tests with Jasmine/Karma

Component & service testing

TestBed, spies, mocks

Build Optimization

Ahead-of-time (AOT) compilation

Tree shaking

Lazy loading

Angular Universal (SSR)

Server-side rendering for SEO

Deployment

Build with ng build --prod

Deploy to Firebase, Vercel, Netlify, or S3